@model IEnumerable<TGProyectoG.Data.UnidadAcademica>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Departamento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Departamento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdUnidadAcademica }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdUnidadAcademica }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdUnidadAcademica })
        </td>
    </tr>
}

</table>
